---
import Layout from '../layouts/Layout.astro';
import { getPublishedPosts } from '../utils/posts';

const posts = await getPublishedPosts();
---

<Layout title="CopyMyJS - Useful JavaScript & TypeScript Snippets">
  <main class="max-w-3xl mx-auto px-4 py-8">
    <div class="text-center mb-16">
      <h1 class="text-5xl font-bold mb-6 text-white">
        { /** Copy<span class="text-[#f7df1e]">My</span><span class="text-[#3178c6]">JS</span> **/ }
        Copy My 
          <span class="bg-[#f7df1e] text-black font-bold px-1.5 py-0.25 small-caps rounded-md">js</span> / <span class="bg-[#3178c6] text-white font-bold px-1.5 py-0.25 small-caps  rounded-md">ts</span>
      </h1>
      <div class="bg-gray-800/50 p-8 rounded-lg border border-gray-700 mb-8">
        <p class="text-xl text-gray-300 mb-4">
          A collection of battle-tested, copy-paste ready 
          {/* <span class="bg-[#f7df1e] text-black font-bold px-1.5 py-0.25 small-caps">js</span> /
          <span class="bg-[#3178c6] text-white font-bold px-1.5 py-0.25 small-caps">ts</span> */} snippets 
          for your projects.
        </p>
        <p class="text-gray-400">
          Each snippet comes with live examples, TypeScript types, and zero dependencies. 
          Just copy, paste, and code away!
        </p>
      </div>
    </div>
    
    <div class="space-y-8">
      {posts.map(post => (
        <article class="border border-gray-700 rounded-lg p-6 hover:border-gray-500 transition-colors">
          <a href={`/blog/${post.slug}`} class="block">
            <h2 class="text-2xl font-bold text-white mb-2">{post.data.title}</h2>
            <p class="text-gray-400 mb-4">{post.data.description}</p>
            <div class="text-sm text-gray-500">
              Published on {post.data.publishDate}
            </div>
          </a>
        </article>
      ))}
    </div>
  </main>
</Layout>