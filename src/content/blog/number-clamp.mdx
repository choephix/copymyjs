---
title: "Number Clamp Utility"
publishDate: "2024-03-20"
description: "Clamp numbers between minimum and maximum values"
---

import CodeSnippet from '../../components/CodeSnippet.astro';
import Example from '../../components/Example.astro';

# Number Clamp Utility

Need to ensure a number stays within a specific range? Here's a utility function for that:

<CodeSnippet file="number/clamp.ts" />

### Live Example

Move the slider to see how values are clamped between 25 and 75:

<Example component="clamp-example" />